CREATE DATABASE IF NOT EXISTS coloc_app CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
-- Les tables sont créées automatiquement par SQLAlchemy au démarrage de l'API (Base.metadata.create_all).
-- Ce fichier sert seulement à créer la base si elle n'existe pas encore.
